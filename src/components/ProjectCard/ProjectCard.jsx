import styles from './ProjectCard.module.css';

function ProjectCard({ title, description, image, projectUrl }) {
  return (
    <div className="project-card">

    </div>
  );
}

export default ProjectCard;